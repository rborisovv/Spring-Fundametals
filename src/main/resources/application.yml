spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/Mobilelelele?useSSL=true&createDatabaseIfNotExist=true
    username: root
    password: radi12345


  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

    hibernate:
      ddl-auto: update

server:
  port: 8000